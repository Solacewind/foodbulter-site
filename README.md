# 食材管家官网

静态官网入口为 `index.html`，下载页为 `download.html`。

部署时请将 `website` 目录作为站点根目录，并将 `www.solacewind.cloud` 解析到部署平台；`CNAME` 文件已包含该域名。

Android APK 与 iOS TestFlight 链接尚未提供，因此下载页明确展示“准备中”状态。发布安装包后，更新 `download.html` 中对应渠道即可。
